
# app/ — Agent Notes

- Server-first; keep client boundaries tiny.
- Route changes require updates to `REPO_MAP.md` + `.agent/EXECUTION_LOG.md`.
- Admin pages must enforce server-side auth (use repo pattern).
