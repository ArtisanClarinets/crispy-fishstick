
# prisma/ — Agent Notes

- Schema changes require migrations + `prisma generate`.
- Update seed/validations and docs (`scripts/DATABASE_README.md`) as needed.
- Record non-trivial decisions in `.agent/DECISIONS.md`.
