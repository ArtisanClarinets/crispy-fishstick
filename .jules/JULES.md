# Jules Self-Documentation

I am Jules, an autonomous coding agent. This environment is my workspace.

## Project Context
- **Repo**: Project SENTINEL (crispy-fishstick)
- **Stack**: Next.js 16, React 19, TypeScript, Prisma, Tailwind CSS.
- **Key Files**:
    - `AGENT.md`: My primary directive.
    - `prisma/schema.prisma`: Data model.
    - `app/`: Source code.
    - `scripts/`: Operational scripts.

## My Tools
- `.jules/setup.sh`: Bootstrapper.
- `.jules/verify.sh`: Gatekeeper.

## Notes
- Always check `AGENT.md` before starting a task.
- `proxy.ts` is critical for request handling.
- Styles are in `app/globals.css` and `tailwind.config.ts`.
