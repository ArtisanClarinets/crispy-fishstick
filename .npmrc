# Keep CI output clean and deterministic
# - `audit=false` prevents install-time vulnerability summaries from failing strict “no warnings” gates
# - `fund=false` disables funding notices
# - `loglevel=error` suppresses transitive deprecation warnings from npm

audit=false
fund=false
loglevel=error
progress=false
